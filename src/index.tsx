import * as React from "react";
import { AppRegistry, View } from "react-native";
import LottieView from "lottie-react-native";

class App extends React.Component {
    public render() {
        return (<View></View>);
    }
}

AppRegistry.registerRunnable("lottieApp", () => App);
